apiVersion: v1
kind: Pod
metadata:
  name: cude-test
spec:
  containers:
    - name: cuda-test
      image: "registry.k8s.io/cud-vector-add:v0.1"
      resources:
        limits:
          nvidia.com/gpu: 1
  nodeSelector:
    accelerator: nvidia-tesla-p100